1，编写一个静态网站服务器
    a.直接打开bat文件，就可以直接将当前文件夹变成一个网站的根目录
        端口直接访问，直接以当前文件夹作为网站根目录

    √b.可以编写配置文件，config.js
        配置文件里面包括配置当前根目录，可以配置的目录为当前路径下的目录

    c.访问目录时，搜索当前目录下的所有目录和文件，并且以html文件的形式返回，让用户可以选择要访问哪个目录。